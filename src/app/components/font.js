import { Roboto, Lato, DMSans, Cormorant, Inconsolata } from "next/font/google";

export const roboto = Roboto({
  subsets: ["latin"],
  display: "swap",
});

export const dmSans = DMSans({
  subsets: ["latin"],
  display: "swap",
});

export const cormorant = Cormorant({
  subsets: ["latin"],
  display: "swap",
});

export const lato = Lato({
  subsets: ["latin"],
  display: "swap",
});

export const inconsolata = Inconsolata({
  subsets: ["latin"],
  display: "swap",
});
